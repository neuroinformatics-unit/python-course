# (no title)

- Creating objects
- class Animal():
- def __init__(self, species):
- self.species = species
- pingu = Animal("penguin")
- print(pingu.species)
- Try adding a new attribute passed on instantiation, ‘noise’
- Create a new method ‘make_noise’ that prints out the noise attribute

## Notes

Describe what a constructor is (a special method, that is called when we create an instance of the object), relate Animal() to __init__()
