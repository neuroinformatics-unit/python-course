# When creating an array all lists must be the same shape

- If we combine two lists to create an array both must have the same length
- If there are two lists there are two rows
- There must be a constant number of columns
