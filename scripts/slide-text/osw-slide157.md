# (no title)

- Exercise
- def find_length(x):
- try:
- return len(x)
- except TypeError:
- return None
- print(find_length("ten"))
- print(find_length(10))
- Create function to return the length of the input
- Return None if the object has no length
