# What is the terminal

- Also referred to as the command prompt (if using conda it is conda prompt) or the command line
- The operating system has a graphical user interface (GUI)
- You can also interact with it via text
- Most applications have a GUI
- Some also have a command line interface (CLI)
- Some only have a CLI (like python)
