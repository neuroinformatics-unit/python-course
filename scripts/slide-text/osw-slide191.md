# (no title)

- Exercise
- cell_0 = Cell(300, 200)
- ratio = cell_0.relative_expression()
- print(ratio)
- cell_0 = Cell(300, 200, 600)
- max_exp = cell_0.max_expression()
- print(max_exp)
- To the Cell class add a  “BFP” attribute
- Add a method to calculate the maximum expression (from GFP, RFP, BFP)
- Use the “max(a, b, c)” syntax
