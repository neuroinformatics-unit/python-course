# (no title)

- Documentation - comments
- def analyse_image(image):
- # run gaussian blur
- image = gaussian(image)
- # run otsu thresholding
- image = otsu(image)
- # run watershed
- image = watershed(image)
- return image
- Sometimes the code itself is not sufficient
- Use comments to add why you did something.
- Use # for comments
- What’s wrong with this example?
