# (no title)

- Creating objects
- class Animal():
- def __init__(self, species):
- self.species = species
- pingu = Animal("penguin")
- print(pingu.species)
- We use class to define objects
- __init__ is a special method that’s called when you create an object
- self is a special argument referring to the specific instance of that class

## Notes

Describe what a constructor is (a special method, that is called when we create an instance of the object), relate Animal() to __init__()
