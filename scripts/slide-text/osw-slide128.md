# (no title)

- Exercise
- def check_passwd_length(passwd):
- if len(passwd) >= 8:
- print("OK!")
- else:
- print("Too short!")
- check_passwd_length("short")
- check_passwd_length("quitelong")
- Write a function to check that a password (e.g. string) is at least 8 characters
