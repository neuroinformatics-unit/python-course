# (no title)

- Function return values
- def c_func(a, b, c):
- sum1 = a + b
- sum_all = a + b + c
- return sum1, sum_all
- result1, result2 = c_func(10, 20, 5)
- result = c_func(10, 20, 5)
- _, other_value = c_func(10, 20, 5)
- Functions can return one or more values
- The values to return are signified by the return keyword
- Some functions don’t return anything!

## Notes

Functions that return multiple things ‘_’
