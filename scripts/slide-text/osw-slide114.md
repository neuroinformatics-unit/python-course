# (no title)

- Using a Python package:
- To work with tabular data
- E.g.: read a csv file into a dataframe df
- import pandas
- df = pandas.read_csv("out.csv")
- print(df)

## Notes

We read the csv file we wrote 

Link on website to download a sample csv if needed
