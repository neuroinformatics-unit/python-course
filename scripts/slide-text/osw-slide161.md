# (no title)

- Structuring your code
- from my_funcs import square_add_10, \
- print_twice
- print(square_add_10(2))
- print_twice("hello")
- Now we can reuse them in a separate script ♻️
- Modularity promotes reuse

## Notes

Create fns in a separate file
Call it in another script
