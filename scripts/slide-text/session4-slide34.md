# Reading a CSV file with pandas

- Most data will be distributed as csv (sometimes excel)
- If it’s in excel you will have to pip install an extra package (it will tell you which one when you try to open the excel file in pandas)
