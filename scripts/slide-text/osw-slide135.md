# (no title)

- Exercise
- def compare_len(a, b="default"):
- if len(a) > len(b):
- return a
- else:
- return b
- longest = compare_len("dog", b="lion")
- print(longest)
- Write a function with one positional and one keyword argument
- Return the longest input argument
