# User created libraries

- There are many community created libraries
- These can be installed with pip
- pip stands for “pip installs packages”
- We run pip in the terminal, as “pip install package_name”
