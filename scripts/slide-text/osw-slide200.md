# (no title)

- Reading and writing text files
- fileobj = open("test.txt", "r")
- contents = fileobj.read()
- fileobj.close()
- print(contents)
- To read the file back, we open it again in reading (r) mode
