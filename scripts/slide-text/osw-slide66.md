# (no title)

- Dictionaries
- my_dict =
- {'a_number': 1,
- 'number_list': [1, 2, 3],
- 'string': "Im in a dict"}
- print(my_dict.keys())
- print(my_dict.values())
- print(my_dict.items())
- Can assign each element (“value”) a label (“key”)
- Note “keys” must be unique
